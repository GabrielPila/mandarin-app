# Archived docs

Completed plans and one-off analyses, kept for history. These are **not** the
active plan — for current work see `docs/INTEGRATION-PLAN.md` (integration) and
`docs/EXTRACTION-PLAN.md` (extraction reference).

- **PLAN.md** — original modularization + 9-features plan. Fully implemented
  (see its "Handoff status": all milestones done and verified). Point-in-time
  record; may reference paths that have since changed (e.g. `css/app.css`,
  now split into `css/theme.css` + `css/layout.css` + `css/components.css`).
- **competitor_analysis.md** — one-off market/competitor deep-dive (Jul 2026).
- **ui_ux_evaluation.md** — one-off UI/UX expert review (Jul 2026).
